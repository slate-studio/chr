test:
  sessions:
    default:
      database: chr_test
      hosts:
      - localhost:27017
      options:
      max_retries: 1
      retry_interval: 0

development:
  sessions:
    default:
      database: chr_develop
      hosts:
        - localhost:27017
      options:
